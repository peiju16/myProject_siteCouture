{% extends 'template.html.twig' %}

{% block title %}Kaëlysse Couture - Inscription{% endblock %}

{% block body %}
    <div class="container" id="reservation">
        <a href="{{ path('app_home') }}" class="p-5"><i class="fa fa-arrow-left"></i> Retour</a>
        <div class="row justify-content-center ">
            <div class="col-lg-10"> 
                {{ include('flash.html.twig') }}
                {{ form_errors(registrationForm) }}  
                <div class="contact-form">
                    {{ form_start(registrationForm, { 'attr': {'novalidate': 'novalidate', 'id': 'contact', 'class':'m-0'} }) }}
                    <div class="row">
                        <div class="col-lg-12">
                            <h4>Inscription</h4>
                        </div>
                        {{ form(registrationForm, { 'attr': {'class':'d-flex flex-wrap col-lg-12 align-items-baseline justify-content-between'} }) }} 
                    </div> 
                    {{ form_end(registrationForm) }}
                </div>
            </div>
        </div>
    </div>


{% endblock %}
