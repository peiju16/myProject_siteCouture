<!-- ***** Header Area Start ***** -->
<header class="header-area header-sticky">
        <div class="container p-0">
            <div class="row">
                <div class="col-12">
                    <nav class="main-nav">
                        <!-- ***** Logo Start ***** -->
                        <a href="{{ path('app_home') }}" class="logo">
                            <img src="{{ asset('assets/images/kaelyss-logo.JPG') }}" align="Kaëlyss Couture" style="width: 150px; height: 80px;">
                        </a>
                        <!-- ***** Logo End ***** -->
                        <!-- ***** Menu Start ***** -->
                        <ul class="nav">
                            <li class="scroll-to-section"><a href="{{ path('app_home') }}" class="active">Accueil</a></li>                        
                            <li class="submenu">
                                <a href="javascript:;">Nos produits</a>
                                <ul>
                                    <li><a href="{{ path('app_product') }}">Nos produits</a></li>
                                    <li><a href="{{ path('app_formation') }}">Nos formations</a></li>
                                </ul>
                            </li>
                            {% if not app.user %}
                            <li class="scroll-to-section"><a href="{{ path('app_registration') }}">Inscription</a></li>
                            {% endif %}  
                            <!-- <li class=""><a rel="sponsored" href="https://templatemo.com" target="_blank">External URL</a></li> -->
                            <li class="scroll-to-section"><a href="#reservation">Contactez-nous</a></li> 
                            <li class="submenu">
                                <a href="javascript:;">Mon Compte</a>
                                <ul>
                                    <li><a href="{{ path('app_login') }}">Se Connecter</a></li>
                                    {% if app.user %}
                                    <li><a href="{{ path('app_user') }}">Profile</a></li>
                                    <li><a href="{{ path('app_cart') }}">Mon Panier</a></li>
                                    <li><a href="{{ path('app_logout') }}">Se Déconnecter</a></li>
                                    {% endif %}
                                </ul>
                            </li>
                            {% if app.user and 'ROLE_ADMIN' in app.user.getRoles() %}                         
                            <li><a href="{{ path('admin') }}">Back Office</a></li>
                            {% endif %}
                        </ul>        
                        <a class='menu-trigger'>
                            <span>Menu</span>
                        </a>
                        <!-- ***** Menu End ***** -->
                    </nav>
                </div>
            </div>
        </div>
    </header>
    <!-- ***** Header Area End ***** -->