{% extends 'template.html.twig' %}

{% block title %}Kaëlysse Couture - Modifier Mot de Passe{% endblock %}

{% block body %}
    <div class="container" id="reservation">
        <div class="row justify-content-center ">
            <div class="col-lg-10"> 
                <a href="{{ path('app_user') }}" type="button" class="main-button-icon"><i class="fa fa-arrow-left"></i> Retour</a>
                {{ include('flash.html.twig') }}
                {{ form_errors(resetPasswordForm) }}  
                <div class="contact-form">
                    {{ form_start(resetPasswordForm, { 'attr': {'novalidate': 'novalidate', 'id': 'contact', 'class':'m-0'} }) }}
                    <div class="row">
                        <div class="col-lg-12">
                            <h4>Modification Mot de Passe</h4>
                        </div>
                        {{ form(resetPasswordForm, { 'attr': {'class':'d-flex flex-wrap col-lg-12 align-items-baseline justify-content-between'} }) }} 
                    </div> 
                    {{ form_end(resetPasswordForm) }}
                </div>
            </div>
        </div>
    </div>


{% endblock %}